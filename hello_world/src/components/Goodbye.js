import React from 'react';

class Goodbye extends React.Component {

        constructor(){
            super();
        }
        
        render(){
            //define return function
            
            return(
                <p>We don't use these components anymore...</p>
            )
        }
}

export default Goodbye;

//this in functionally the same as the hello.js but this is the OLD version